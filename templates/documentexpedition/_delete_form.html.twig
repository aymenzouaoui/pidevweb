<form method="post" action="{{ path('app_documentexpedition_delete', {'id': documentexpedition.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ documentexpedition.id) }}">
    <button class="btn">Supprimer</button>
</form>
