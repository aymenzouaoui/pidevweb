<form method="post" action="{{ path('app_vehicule_delete', {'idV': vehicule.idV}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vehicule.idV) }}">
    <button class="btn">Supprimer</button>
</form>
